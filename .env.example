# Deployer / agent key. Studio Network is gasless, so this needs no balance there.
CASSANDRA_DEPLOYER_KEY=0x...
# Optional: only used to enrich the phrasing of agent-generated questions.
# The agent works fully without it.
OPENROUTER_API_KEY=
