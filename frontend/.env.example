# Reown (WalletConnect) project id for the connect modal.
VITE_REOWN_PROJECT_ID=
# Contract addresses are written into src/lib/contractAddresses.ts by the deploy
# script, so a fresh deployment always reaches the app.
